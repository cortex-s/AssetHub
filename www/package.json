{
  "name": "www",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "live-server . --port=5000",
    "build": "rm -rf validators && cp -r ../shared/validators ./validators",
    "start": "npm run build && serve . -p 5000"
  },
  "dependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.6"
  }
}
